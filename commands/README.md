# Commands

This directory contains custom slash commands for Claude Code.

## Available Commands

*No commands yet. Add your custom commands here.*

## How to Add a Command

Create a new directory with the following structure:

```
commands/
└── your-command/
    ├── .claude-plugin/
    │   └── plugin.json
    ├── commands/
    │   └── your-command.md
    └── README.md
```
